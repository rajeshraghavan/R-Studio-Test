installed.packages()[1:50,]
installed.packages()

installed.packages(lib.loc = NULL, priority = NULL,
                   noCache = FALSE, fields = NULL,
                   subarch = .Platform$r_arch)

version
packageStatus()